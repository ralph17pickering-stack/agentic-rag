# Create a new developer role with a specific prompt for Claude Code - once a SPEC.md file has been created

- Responsible for creating tests, producing code, ensuring it passes.
- Should create minimal, specific, reviewable changes per run (module, class, function, file, etc.)
- Produce working, testable code that meets the requirements in SPEC.md